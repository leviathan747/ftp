public service Telnet::Connections.default () return instance of Connections is
begin
  if ( null = find_one Connections() ) then
    return create Connections( id => 1 );
  else
    return find_one Connections();
  end if;
end service;
